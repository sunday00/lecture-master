import React from 'react';
import '../resources/Local.css';

const Local = () => {
    // console.log(styles);
    return (
        <div className="radius">
            <p className={'action'}>test</p>
        </div>
    );
};

export default Local;
