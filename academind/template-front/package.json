{
  "type": "module",
  "scripts": {
    "build": "npx tsc --build",
    "clean": "npx tsc --build --clean"
  },
  "dependencies": {
    "@picocss/pico": "^1.5.10",
    "rxjs": "^7.8.1",
    "typescript": "^5.2.2"
  }
}
